#include "../include/cuda_resources.h"

namespace cudatrader {

// This file exists to satisfy the build system requirements.
// All functions are defined as inline in the header file.

// Dummy function to ensure the file is not empty
void initCudaResources() {
    // This function does nothing, it's just here to ensure the file is not empty
    // and the build system has something to compile.
}

} // namespace cudatrader
